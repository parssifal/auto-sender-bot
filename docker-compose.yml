services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: auto-sender-bot:local
    restart: unless-stopped
    init: true
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/bot.db
      HEALTHCHECK_HOST: 0.0.0.0
      HEALTHCHECK_PORT: 8080
      WEBAPP_HOST: 0.0.0.0
      WEBAPP_PORT: 8081
    ports:
      - 127.0.0.1:8080:8080
      # Mini App(s) — a TLS reverse proxy fronts this. Plan of record: Caddy +
      # DuckDNS for a permanent HTTPS address (see deploy/README.md).
      - 127.0.0.1:8081:8081
    volumes:
      - ./data:/app/data

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    profiles:
      - redis
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data

volumes:
  redis-data:
